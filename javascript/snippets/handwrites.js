function promiseAll(iterable) {
}

class LRUCache {
  constructor(capacity) {
  }
  get(key) {
  }
  put(key, value) {
  }
}

class EventEmitter {
  on(event, handler) {
  }
  once(event, handler) {
  }
  off(event, handler) {
  }
  emit(event, ...args) {
  }
}

function createScheduler(maxConcurrency, retry) {
  return {
    add(taskFn) {
    },
    onIdle() {
    }
  };
}

module.exports = {
  promiseAll,
  LRUCache,
  EventEmitter,
  createScheduler,
};
