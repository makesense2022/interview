1、
1、幽灵依赖是项目代码中依赖了一个不存在的依赖，但是却能工作。主要原因是因为依赖的依赖是这个包，恰恰就能用上了。核心原因是 npm 按照的node_modules 实际上上平铺的。导致只要在 node_modules里面的包都能用。
2、pnpm 使用硬链接和软连接，在node_modules里面没有非package.json 下的依赖，全都是package.json 下的依赖，node_modules下的依赖都是软连接，实际执行的是 .pnpm隐藏文件夹下的硬链接，而硬链接指向的是电脑中的唯一安装包。
3、问题：因为是依赖的依赖导致了 幽灵依赖。如果直接依赖做的修改，不依赖这个包之后，工程升级这个包之后，就找不到这个依赖包了，这样就会报错。导致构建失败。也可能存在版本不一致后，生产环境出现报错。找不到方法。
======
2、硬链接和软连接
1、硬链接是指向真正项目的一个句柄，只要项目不删除，即使挪动位置，硬链接也能找到
软连接是一个快捷方式，指向真正项目的路径地址，但是只要项目挪动，软连接就找不到真正项目了。
2、硬链接像一个书的目录，通过这个目录可以找到书在图书馆的任何位置。而软连接更像是一个地址，记录书在书架的几排几号，但是书挪动了就找不到了。
3、pnpm用软连接在 node_modules 下展示实际依赖，而真是依赖的所有包在.pnpm 文件夹下。
好处是大大节省了磁盘空间 。

=======
3. 
monorepo 是一个项目下集成多个不同场景不同类型的子应用。子应用直接可以毫无关系。
但是子应用之间，可以使用共享的库，比如 lib 和 ui 组件。
workspace 将 整个monorepo 下的 应用都看成一个包。
即使包和包直接互相依赖，也能保证依赖的唯一性。防止出现不同依赖版本的问题。
pnpm 可以通过软连接的方式，管理多个包之间的依赖关系。



