1、
问题1：通常我们会用axios等统一拦截器去处理。
我们会有一个 loop 函数，里面实现循环几次，间隔多少。
我们一般用promise或者 promiseAll 等能力处理并发请求。

问题2：
一般用 按钮 loading 来阻止。
axios 封装了 基于xhr 底层 abort的能力，我在调用的时候，先用 axios 去cancel 下就行。


问题3：
一些查询get接口，我们可以考虑缓存。
需要后端配置接口缓存。或者 nginx配置。 通过设置 expires 来设置缓存失效。 因为是接口不是文件，所以可能没办法用 etag 来缓存。

=======
2、当状态在多个节点，或者说是页面需要共享的时候